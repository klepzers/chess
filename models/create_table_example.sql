-- Create your table using this example
CREATE TABLE your_table_name (
    id int(11) AUTO_INCREMENT PRIMARY KEY, -- Identification key to be created differently due to 2-player implementation
    positions varchar(255) NOT NULL -- Do not change this variable
);
